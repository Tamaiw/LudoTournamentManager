apiVersion: v1
kind: Secret
metadata:
  name: ludo-secrets
type: Opaque
stringData:
  # REQUIRED - Replace with actual values before applying
  db-password: REPLACE_WITH_SECURE_PASSWORD
  database-url: postgres://ludo:REPLACE_WITH_SECURE_PASSWORD@postgres:5432/ludo_tournament
  jwt-secret: REPLACE_WITH_SECURE_JWT_SECRET